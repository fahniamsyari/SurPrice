language: php

php:
  - 7.2

branches:
    only:
        - master 
        #QA/Staging
        - develop 
        #DevServer

before_script:
  composer install --no-interaction

script:
  -vendor/bin/phpunit